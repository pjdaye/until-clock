<Window x:Class="UntilClock.SetTargetWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Set Target Date" Height="200" Width="360"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Target date:" Margin="0,0,0,4"/>
        <DatePicker Grid.Row="1" x:Name="DatePickerControl"
                    SelectedDateFormat="Long"
                    Margin="0,0,0,12"/>

        <TextBlock Grid.Row="2" Text="(Time defaults to midnight on selected date)"
                   Foreground="Gray" FontSize="11" VerticalAlignment="Bottom" Margin="0,0,0,8"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Width="80" Margin="0,0,8,0" IsCancel="True" Click="Cancel_Click"/>
            <Button Content="Confirm" Width="80" IsDefault="True" Click="Confirm_Click"/>
        </StackPanel>
    </Grid>
</Window>
