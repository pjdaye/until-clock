<Application x:Class="UntilClock.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <!-- Colors -->
        <Color x:Key="BackgroundColor">#FF1A1A1A</Color>
        <Color x:Key="AccentColor">#FF007FFF</Color>
        <Color x:Key="SecondaryTextColor">#FF888888</Color>
        <Color x:Key="CompleteBackgroundColor">#FF007FFF</Color>
        <Color x:Key="CompleteForegroundColor">#FF111111</Color>

        <SolidColorBrush x:Key="BackgroundBrush" Color="{StaticResource BackgroundColor}"/>
        <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource AccentColor}"/>
        <SolidColorBrush x:Key="SecondaryTextBrush" Color="{StaticResource SecondaryTextColor}"/>
        <SolidColorBrush x:Key="CompleteBackgroundBrush" Color="{StaticResource CompleteBackgroundColor}"/>
        <SolidColorBrush x:Key="CompleteForegroundBrush" Color="{StaticResource CompleteForegroundColor}"/>
        <SolidColorBrush x:Key="WhiteBrush" Color="White"/>

        <!-- Base window style -->
        <Style TargetType="Window">
            <Setter Property="Background" Value="{StaticResource BackgroundBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource WhiteBrush}"/>
        </Style>
    </Application.Resources>
</Application>
